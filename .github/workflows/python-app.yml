name: Ejecutar Bot

on:
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Instalar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar herramientas 
        run: pip install requests yfinance

      - name: Ejecutar bot
        run: python bot.py
        env:
        TELEGRAM_TOKEN: 8980218948:AAF3SmcMxlz4aYXjAoJXFnQ_FfM98_e6Zic
   TELEGRAM_CHAT_ID:   6219209932
